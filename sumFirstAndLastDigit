public static int sumFirstAndLastDigit(int number) {
        // sum first and last digit of input
        if(number < 0) {
            return -1;
        }

        int finalDigit = number % 10;

        while(number > 9) {
            number /= 10;
        }
        return number + finalDigit;
    }
